apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-argo.fullname" . }}
  labels:
    {{- include "demo-argo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-argo.selectorLabels" . | nindent 4 }}
