apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "demo-argo.fullname" . }}-test-connection"
  labels:
    {{- include "demo-argo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "demo-argo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
